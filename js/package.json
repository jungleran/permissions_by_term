{
  "name": "pbt",
  "version": "1.0.0",
  "description": "JavaScript code has been tested with [Jasmine](https://jasmine.github.io/). Install the dependencies by [Yarn](https://yarnpkg.com/lang/en/). Just run `yarn`. To run the tests, switch to the `js` folder and run `yarn test`.",
  "main": "node-form.js",
  "dependencies": {
    "babel-preset-es2015": "^6.24.1",
    "chromy": "^0.5.11",
    "sinon": "^5.0.10",
    "babel-cli": "^6.26.0"
  },
  "scripts": {
    "test": "qunit *.tests.js",
    "build": "babel **/*.js -d babel-compiled --ignore node_modules",
    "build-and-test": "rm -rf babel-compiled/ && npm run-script build && qunit babel-compiled/test/access-creator.tests.js"
  },
  "author": "",
  "license": "ISC",
  "babel": {
    "ignore": [
      "node_modules",
      "package.json",
      "package-json.lock"
    ],
    "presets": [
      "es2015"
    ]
  }
}
