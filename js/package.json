{
  "name": "pbt",
  "version": "1.0.0",
  "description": "JavaScript code has been tested with [Jasmine](https://jasmine.github.io/). Install the dependencies by [Yarn](https://yarnpkg.com/lang/en/). Just run `yarn`. To run the tests, switch to the `js` folder and run `yarn test`.",
  "main": "node-form.js",
  "dependencies": {
    "qunit": "^2.6.1",
    "sinon": "^5.0.10"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0"
  },
  "scripts": {
    "test": "qunit *.tests.js",
    "build": "babel ./**/*.js -d lib --ignore node_modules"
  },
  "author": "",
  "license": "ISC",
  "babel": {
    "ignore": [
      "node_modules",
      "package.json",
      "package-json.lock"
    ]
  }
}
