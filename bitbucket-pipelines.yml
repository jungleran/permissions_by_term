# This is a sample build configuration for PHP.
# Check our guides at https://confluence.atlassian.com/x/e8YWN for more examples.
# Only use spaces to indent your .yml configuration.
# -----
# You can specify a custom docker image from Docker Hub as your build environment.
image: derh4nnes/pipeline-behat:latest
clone:
  depth: full

pipelines:
  default:
    - step:
        name: Build and test
        caches:
          - composer
        script:
          - apt-get update
          - apt-get install -y screen php7.1-sqlite libsqlite3-dev sqlite3
          - cd ..
          - mkdir config_replace/
          - mv build/* config_replace/
          - mv config_replace/ build/
          - cd build
          - composer create-project drupal-composer/drupal-project:8.x-dev drupal --stability dev --no-interaction
          - mv permissions_by_term drupal/web/modules/
          - (cd drupal/web/modules/permissions_by_term/tests/ && ../../../../vendor/bin/phpunit)
    - step:
        name: Deploy
        script:
          - apt-get update && apt-get install -y unzip git
          - git remote add d.o jepSter@git.drupal.org:project/config_replace.git
          - git config --global push.default simple
          - git config --global user.name "Peter Majmesku"
          - git push -u d.o
          - git push --tags